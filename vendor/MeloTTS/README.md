# MeloTTS Vendor Directory

将 `https://github.com/myshell-ai/MeloTTS` 的源码放到本目录。

推荐结构：

```text
vendor/MeloTTS/melo/
vendor/MeloTTS/setup.py
vendor/MeloTTS/requirements.txt
```

项目运行时会优先从这里导入 `melo.api.TTS`。
